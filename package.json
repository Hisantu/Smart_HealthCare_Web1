{
  "name": "smart-health-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd web && npm install --production=false && npm run build && cd ../server && npm install",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}